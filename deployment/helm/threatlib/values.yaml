image:
  repository: ghcr.io/blackplane-systems/threatlib
  tag: latest
  pullPolicy: IfNotPresent

api:
  replicas: 2
  port: 8000

dashboard:
  enabled: true
  port: 8501

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi
